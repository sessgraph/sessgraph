# OpenAPI 契约

> 状态: 占位
> 最近更新: 2026-06-12

SessGraph P0 不包含 Web server 或 HTTP API，因此当前没有 OpenAPI 契约。

当未来引入 server mode 或对外 HTTP API 时：

1. 先在 `docs/state/pr-queue.md` 立项；
2. 如改变公开契约或架构边界，先写 ADR/RFC；
3. 在本目录提交 OpenAPI 文件；
4. 增加并运行契约漂移检查。
